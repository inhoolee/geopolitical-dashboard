# ACLED is file-based in this repo.
# Drop regional aggregate CSV snapshots into:
# data/raw/acled/*_aggregated_data_up_to-*.csv
# Files named number_of_*.csv and *.xlsx are ignored by the pipeline.

# Optional: override default warehouse path
# WAREHOUSE_PATH=/custom/path/geopolitical.duckdb
